<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a {% if app.request.pathinfo == "/index" %} class="nav-link active"{% else %} class="nav-link" {% endif %} aria-current="page" href="./index">Accueil</a>
        {# <a {% if app.request.pathinfo == "/profil" %} class="nav-link active"{% else %} class="nav-link" {% endif %} href="./profil">Profil</a> #}
        <a {% if app.request.pathinfo == "/addArticle" %} class="nav-link active"{% else %} class="nav-link" {% endif %} aria-current="page" href="./addArticle">Ajouter article</a>
        {% if user is defined %}
        <li class="nav-item dropdown position-absolute top-1 end-0">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">{{ user.email }}</a>
          <ul class="dropdown-menu">
            <li><a class="nav-link" href="./profil"> <button type="button" class="btn btn-primary">Profil</button></a></li>
            <li><a class="nav-link" href="./logout"> <button type="button" class="btn btn-danger">Deconnexion</button></a></li>
          </ul>
        </li>
        {% elseif app.request.pathinfo != "/register" %}
        <a class="nav-link position-absolute top-0 end-0" href="./register"> <button type="button" class="btn btn-primary">Inscription</button></a>
        {% elseif app.request.pathinfo != "/login" %}
        <a class="nav-link position-absolute top-0 end-0" href="./login"> <button type="button" class="btn btn-success">Connexion</button></a>
        {% endif %}
      </div>
    </div>
  </div>
</nav>